import { extrema } from "./helpers/quickCheck";

describe("quickCheck", () => {
  it("should ", async () => {
    Object.keys(extrema).map(k => console.log(`${k}\t= ${extrema[k].toString(16)}`));
  });
});
