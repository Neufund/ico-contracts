pragma solidity 0.4.15;


contract MAllowance {

    ////////////////////////
    // Internal functions
    ////////////////////////

    function mAllowanceTransfer(address from, address to, uint256 amount)
        internal
        returns (bool);

}
